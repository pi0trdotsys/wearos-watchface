<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <service
            android:name=".EnergyScoreComplicationService"
            android:directBootAware="true"
            android:exported="true"
            android:icon="@drawable/ic_energy_score"
            android:label="@string/energy_score_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
            <meta-data
                android:name="androidx.watchface.complications.datasource.DEFAULT_CONFIG_SUPPORTED"
                android:value="true" />
        </service>

        <!-- No <data> element here: BIND_LISTENER is how Play Services binds
             this service to deliver onDataChanged for ANY path, system-wide
             - it is not itself carrying our path, so a <data> filter here
             stops the binding intent from ever matching and the service
             never gets bound at all. Path filtering happens where it
             belongs, in EnergyScoreDataListenerService.onDataChanged(). -->
        <service
            android:name=".EnergyScoreDataListenerService"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER" />
            </intent-filter>
        </service>
    </application>
</manifest>
